#include <iostream>
using namespace std;

int main() {
    // Monolithic Program

    // Ex 1: Take 2 nums from keyboard and add them
    int a, b, c;
    cout << "Enter 2 nums";
    cin >> a >> b;
    c = a + b;
    // Then print them to the screen
    cout << "Sum is " << c << endl;
    return 0;
}
